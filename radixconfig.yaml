apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
    name: dot-web
spec:
    build:
    environments:
        - name: prod
        - name: test
          build:
            from: v0*   
        - name: dev
          build:
              from: main|dev-deploy
    components:
        - name: frontend
          src: .
          publicPort: http
          ports:
              - name: http
                port: 3000